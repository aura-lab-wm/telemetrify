#!/usr/bin/env bash
# Launch the FastAPI UI on localhost:8765.
set -eu
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
PORT="${PORT:-8767}"
exec "$ROOT/.venv/bin/python" -m uvicorn prompt_telemetry.ui.app:app \
  --host 127.0.0.1 --port "$PORT" "$@"
