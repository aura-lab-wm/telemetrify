#!/usr/bin/env bash
# Train the local grader classifier from auto_grades + annotations.
set -eu
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec "$ROOT/.venv/bin/python" -m prompt_telemetry.ai.classifier train "$@"
